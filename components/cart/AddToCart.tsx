// Stub — full implementation arrives in Phase 2 (Task 2.3, cart store + WhatsApp checkout).
export function AddToCart(props: { slug: string; nombre: string; precioCOP: number }) {
  void props;
  return null;
}
